*Permission denied*

User *{{ user_name }}* - `{{ user }}`{{ (" " + user_email) if user_email else "" }} tried to access account *{{ account }}* - `{{ account_name }}`.{% if context %}

Context: `{{ context }}`{% endif %}