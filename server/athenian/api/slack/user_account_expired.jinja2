*User accessed expired account on {{ env }}*

We rejected user *{{ user_name }}* - `{{ user }}`{{ (" " + user_email) if user_email else "" }} on account *{{ account }}* - `{{ account_name }}` because it expired on `{{ expired_at }}`.