*New API installation on {{ env }}*

Account {{ account }} has bound to metadata installation{{ "s" if iids|length > 1 else "" }} {{ iids | join(", ") }} ({{ repos|length }} repos):

{% for repo in repos %}
  * {{ repo }}
{% endfor %}
